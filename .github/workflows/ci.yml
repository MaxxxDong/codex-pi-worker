name: CI

on:
  push:
  pull_request:

jobs:
  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - run: node --check macos/lib/events.mjs
      - run: zsh -n macos/bin/pi-worker
      - run: node --test macos/tests/events.test.mjs

  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - run: python -m pip install ruff
      - run: ruff check scripts tests
      - run: python -m unittest discover -s tests -p "test_*.py"
      - run: node --test tests/test_pi_worker_guard.mjs
